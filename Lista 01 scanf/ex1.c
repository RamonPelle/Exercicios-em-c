#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>

/* 1. Faça um programa que leia dois valores inteiros e imprima a diferença entre o primeiro
valor e o segundo valor lido. */
int main(){
    int v1, v2;
    v1 = 2;
    v2 = 1;
    printf("Valor 01 é: %d\n",v1);
    printf("Valor 02 é: %d\n",v2);
    printf("A diferença entre eles é: %d",v1-v2);
    return 0;
}
